<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">

    <link rel="stylesheet" href="/css/global.css">
    <link rel="stylesheet" href="/css/usuarios/styles.css">

    <title>Usuário - <%= dadosUsuario.matricula %></title>
</head>
<body>


    <div class="topnav">

        <a href="http://localhost:3000/">Log Out</a>
    
        <a href="http://localhost:3000/lista-usuarios">Página incial</a>

    </div>
    
    <div class="container-usuario">

        <h1>Usuário - Matrícula <%= dadosUsuario.matricula %></h1>

        <form action="" method="post">

            <div class="titulo-bloco-conteudo">
                <h2>Dados do funcionário</h2>
            </div>

            <div class="bloco-conteudo">
                <div class="linha-conteudo">
                    <h3>Matrícula: </h3>
                    <input type="text" name="matricula" placeholder='Matrícula do profissional' value="<%= dadosUsuario.matricula %>" required></input> 
                </div>
    
                <div class="linha-conteudo">
                    <h3>Nome: </h3>
                    <input  type="text" name="nome" placeholder='Nome do profissional' value="<%= dadosUsuario.nome %>" required></input> 
                </div>
    
                <div class="linha-conteudo">
                    <h3>Função: </h3>
                    <input  type="text" name="funcao" placeholder='Função do profissional' value="<%= dadosUsuario.funcao %>" required></input> 
                </div>
    
                <div class="linha-conteudo">
                    <h3>Empresa: </h3>
                    <input  type="text" name="empresa" placeholder='Empresa contratante' value="<%= dadosUsuario.empresa %>" required></input> 
                </div>
    
                <div class="linha-conteudo">
                    <h3>Perfil: </h3>
                    <select name="perfil" required>
                        <option>--- Escolha perfil do profissional ---</option>
                        <option value="adm" <% if(dadosUsuario.perfil === "adm") { %> selected  <% }%> >Administrador de sistema</option>
                        <option value="encarregado-manutencao" <% if(dadosUsuario.perfil === "encarregado-manutencao") { %> selected  <% }%> >Encarregado de manutenção</option>
                        <option value="fiscal" <% if(dadosUsuario.perfil === "fiscal") { %> selected  <% }%> >Fiscal de contrato</option>
                        <option value="seguranca-trabalho" <% if(dadosUsuario.perfil === "seguranca-trabalho") { %> selected  <% }%> >Profissional de segurança no trabalho</option>
                        <option value="tecnico-manutencao" <% if(dadosUsuario.perfil === "tecnico-manutencao") { %> selected  <% }%> >Técnico de manutenção</option>
                    </select> 
                </div>
            </div>

            
            <div class="botao-alterar">
                <button type="submit">Salvar alterações</button>
            </div>


        </form>

    </div>

</body>
</html>